<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10 ">
    <div class="w-full flex justify-end">
      <a href="/logout" class="bg-red-500 rounded-md px-3 text-small py-2 mb-3">Logout</a>
    </div>

    <h2 class="text-3xl mt-3 mb-5">Edit Your Post</h2>

    <form action="/update/<%= post._id %>" method="post">
      <textarea name="content" class="w-1/3 resize-none bg-transparent border-2 mb-3 p-3 block outline-none border-zinc-800 rounded-md" placeholder="What's on your mind ?"><%= post.content %></textarea>
      <input type="submit" value="Update Post" class="px-3 text-small w-40 block py-2 bg-green-500 rounded-md">
    </form>


   
  </div>
</body>
</html>