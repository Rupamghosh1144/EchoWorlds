<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EchoWords - Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col">
  <div class="w-full flex justify-between items-center px-8 py-4 border-b border-zinc-800 bg-zinc-900">
    <a href="/profile" class="flex items-center gap-2 cursor-pointer">
      <img src="/images/echowords.png" alt="EchoWords Logo" class="h-14 w-auto object-contain">
    </a>
    <div class="flex gap-3">
      <a href="/logout" class="bg-red-500 rounded-md px-4 py-2 text-sm hover:bg-red-600 transition">Logout</a>
      <a href="/profile/upload" class="bg-green-500 rounded-md px-4 py-2 text-sm hover:bg-green-600 transition">Update DP</a>
    </div>
  </div>
  <div class="flex-1 p-10">
    <div class="flex items-center gap-3">
      <div class="w-12 h-12 rounded-md overflow-hidden border border-zinc-700">
        <img src="/images/uploads/<%= user.profilepic %>" alt="" class="w-full h-full object-cover">
      </div>
      <h3 class="text-2xl">
        <span class="font-light">Hello</span>, <%= user.name %> 👋
      </h3>
    </div>
    <h5 class="mb-5 mt-6">You can create a new post 📫</h5>
    <form action="/post" method="post" class="space-y-3">
      <textarea name="content" 
        class="w-1/3 resize-none bg-transparent border-2 p-3 block outline-none border-zinc-800 rounded-md focus:border-blue-500 transition"
        placeholder="What's on your mind ?"></textarea>
      <input type="submit" value="Create new post" 
        class="px-4 py-2 bg-blue-500 rounded-md text-sm hover:bg-blue-600 transition w-40">
    </form>
    <div class="posts mt-12">
      <h3 class="text-zinc-500 mb-4">Your Posts</h3>
      <div class="postcontainer space-y-5">
        <% user.posts.slice().reverse().forEach(function(post){ %>
          <div class="post w-1/3 rounded-md p-4 bg-zinc-700 border-2 border-zinc-800">
            <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
            <p class="text-sm tracking-tight mb-1"><%= post.content %></p>
            <small class="block mb-2">Likes <%= post.likes.length %></small>
            <div class="btns flex gap-4">
              <a href="/like/<%= post._id %>" class="text-blue-500 hover:underline">
                <%= post.likes.map(id => id.toString()).includes(user._id.toString()) ? "Unlike" : "Like" %>
              </a>
              <a href="/edit/<%= post._id %>" class="text-zinc-500 hover:underline">Edit</a>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </div>

</body>
</html>
